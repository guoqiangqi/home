apiVersion: v1
kind: Service
metadata:
  name: home-app-service
  labels:
    app: home-app
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: home-app 